% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{r.}
\alias{r.}
\title{Compute standardized residuals from a linear model}
\usage{
r.(formula, ...)
}
\arguments{
\item{formula}{A model formula passed to \code{\link[stats]{lm}}.}

\item{...}{Additional arguments passed to \code{\link[stats]{lm}}, such as
\code{data}, \code{subset}, or \code{na.action}.}
}
\value{
A numeric vector containing the standardized residuals from the
fitted linear model.
}
\description{
Fits a linear model and returns its standardized residuals.
}
\details{
The function first fits a model using \code{\link[stats]{lm}} and then
applies \code{\link[stats]{rstandard}} to the fitted model.

It is equivalent to:

\code{rstandard(lm(formula, ...))}
}
\examples{
dat <- data.frame(
  y = c(2, 4, 5, 8, 10),
  x = c(1, 2, 3, 4, 5)
)

r.(y ~ x, data = dat)

}
\seealso{
\code{\link[stats]{lm}}, \code{\link[stats]{rstandard}}
}
